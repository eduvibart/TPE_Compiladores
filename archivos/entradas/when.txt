program {
    const a=:5,b=:4;
    i32 o;
    o=:0;
    when (a<b) then{
        fun cumple(i32 a,i32 b): i32 {
            i32 z;
            return (z);
        };
        i32 i;
        i=:88;
    };
    o=:i;
    o=:cumple(a,b);
    when (a>b) then
        
    fun c(): f32 {
        when (a<b) then{
        
        };
        when (a<b) then
               o=:9;     
    };
}